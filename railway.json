{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'gunicorn -w 4 -b 0.0.0.0:${PORT:-8000} app:app'",
    "restartPolicyMaxRetries": 5
  }
}
